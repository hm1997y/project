<view class="container" >
    <image src="./image/circle.png" class="image" hidden="{{like}}" catch:tap="onLike"></image>
    <image src="./image/circle_fill.png" class="image" hidden="{{!like}}" catch:tap="onLike"></image>
</view>
